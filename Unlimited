local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UIS = game:GetService("UserInputService")
local player = Players.LocalPlayer

local KEY = "kairon-script"
local DISCORD = "https://discord.gg/nBBu9eRSws"

local gui = Instance.new("ScreenGui")
gui.Name = "RivoHub"
gui.ResetOnSpawn = false
gui.Parent = game:GetService("CoreGui")

local main = Instance.new("Frame")
main.Size = UDim2.fromOffset(520, 330)
main.Position = UDim2.new(0.5, -260, 0.5, -165)
main.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
main.BorderSizePixel = 0
main.Parent = gui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 14)
corner.Parent = main

local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(85, 85, 110)
stroke.Thickness = 1
stroke.Parent = main

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -30, 0, 45)
title.Position = UDim2.fromOffset(15, 12)
title.BackgroundTransparency = 1
title.Text = "Rivo Hub"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.TextSize = 28
title.Font = Enum.Font.GothamBold
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = main

local subtitle = Instance.new("TextLabel")
subtitle.Size = UDim2.new(1, -30, 0, 30)
subtitle.Position = UDim2.fromOffset(15, 55)
subtitle.BackgroundTransparency = 1
subtitle.Text = "Brookhaven • Key System"
subtitle.TextColor3 = Color3.fromRGB(160, 160, 175)
subtitle.TextSize = 15
subtitle.Font = Enum.Font.Gotham
subtitle.TextXAlignment = Enum.TextXAlignment.Left
subtitle.Parent = main

local info = Instance.new("TextLabel")
info.Size = UDim2.new(1, -50, 0, 55)
info.Position = UDim2.fromOffset(25, 95)
info.BackgroundTransparency = 1
info.Text = "Join the Rivo Hub Discord to get access to the key."
info.TextColor3 = Color3.fromRGB(210, 210, 220)
info.TextSize = 16
info.Font = Enum.Font.Gotham
info.TextWrapped = true
info.Parent = main

local discord = Instance.new("TextButton")
discord.Size = UDim2.fromOffset(210, 42)
discord.Position = UDim2.fromOffset(25, 160)
discord.BackgroundColor3 = Color3.fromRGB(88, 101, 242)
discord.Text = "Join Discord"
discord.TextColor3 = Color3.fromRGB(255, 255, 255)
discord.TextSize = 16
discord.Font = Enum.Font.GothamBold
discord.AutoButtonColor = false
discord.Parent = main

local discordCorner = Instance.new("UICorner")
discordCorner.CornerRadius = UDim.new(0, 9)
discordCorner.Parent = discord

local keyBox = Instance.new("TextBox")
keyBox.Size = UDim2.fromOffset(210, 42)
keyBox.Position = UDim2.fromOffset(25, 215)
keyBox.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
keyBox.PlaceholderText = "Enter key..."
keyBox.PlaceholderColor3 = Color3.fromRGB(120, 120, 135)
keyBox.Text = ""
keyBox.TextColor3 = Color3.fromRGB(255, 255, 255)
keyBox.TextSize = 15
keyBox.Font = Enum.Font.Gotham
keyBox.ClearTextOnFocus = false
keyBox.Parent = main

local keyCorner = Instance.new("UICorner")
keyCorner.CornerRadius = UDim.new(0, 9)
keyCorner.Parent = keyBox

local keyStroke = Instance.new("UIStroke")
keyStroke.Color = Color3.fromRGB(65, 65, 80)
keyStroke.Parent = keyBox

local check = Instance.new("TextButton")
check.Size = UDim2.fromOffset(210, 42)
check.Position = UDim2.fromOffset(260, 215)
check.BackgroundColor3 = Color3.fromRGB(55, 190, 105)
check.Text = "Unlock Rivo Hub"
check.TextColor3 = Color3.fromRGB(255, 255, 255)
check.TextSize = 15
check.Font = Enum.Font.GothamBold
check.AutoButtonColor = false
check.Parent = main

local checkCorner = Instance.new("UICorner")
checkCorner.CornerRadius = UDim.new(0, 9)
checkCorner.Parent = check

local status = Instance.new("TextLabel")
status.Size = UDim2.new(1, -50, 0, 25)
status.Position = UDim2.fromOffset(25, 270)
status.BackgroundTransparency = 1
status.Text = "Status: Waiting for key"
status.TextColor3 = Color3.fromRGB(150, 150, 165)
status.TextSize = 13
status.Font = Enum.Font.Gotham
status.TextXAlignment = Enum.TextXAlignment.Left
status.Parent = main

local function hover(button, normal, hoverColor)
	button.MouseEnter:Connect(function()
		TweenService:Create(button, TweenInfo.new(0.15), {
			BackgroundColor3 = hoverColor
		}):Play()
	end)

	button.MouseLeave:Connect(function()
		TweenService:Create(button, TweenInfo.new(0.15), {
			BackgroundColor3 = normal
		}):Play()
	end)
end

hover(discord, Color3.fromRGB(88, 101, 242), Color3.fromRGB(105, 116, 255))
hover(check, Color3.fromRGB(55, 190, 105), Color3.fromRGB(65, 210, 120))

discord.MouseButton1Click:Connect(function()
	if setclipboard then
		setclipboard(DISCORD)
		status.Text = "Discord invite copied to clipboard."
	else
		status.Text = "Discord: " .. DISCORD
	end
end)

check.MouseButton1Click:Connect(function()
	if keyBox.Text == KEY then
		status.Text = "Key verified! Rivo Hub unlocked."
		status.TextColor3 = Color3.fromRGB(70, 220, 120)

		TweenService:Create(main, TweenInfo.new(0.35, Enum.EasingStyle.Quad), {
			Size = UDim2.fromOffset(520, 360)
		}):Play()

		task.wait(0.4)

		info.Text = "Welcome to Rivo Hub!\nYour key has been accepted."

		keyBox.Visible = false
		check.Visible = false
		discord.Visible = false

		local continueButton = Instance.new("TextButton")
		continueButton.Size = UDim2.fromOffset(300, 48)
		continueButton.Position = UDim2.fromOffset(110, 220)
		continueButton.BackgroundColor3 = Color3.fromRGB(80, 120, 255)
		continueButton.Text = "Enter Rivo Hub"
		continueButton.TextColor3 = Color3.fromRGB(255, 255, 255)
		continueButton.TextSize = 17
		continueButton.Font = Enum.Font.GothamBold
		continueButton.Parent = main

		local c = Instance.new("UICorner")
		c.CornerRadius = UDim.new(0, 10)
		c.Parent = continueButton

		continueButton.MouseButton1Click:Connect(function()
			info.Text = "Rivo Hub is ready."
			status.Text = "Best of luck!"
			continueButton.Text = "Rivo Hub Loaded"
			continueButton.BackgroundColor3 = Color3.fromRGB(55, 190, 105)
		end)
	else
		status.Text = "Invalid key. Please get the key from Discord."
		status.TextColor3 = Color3.fromRGB(255, 90, 90)

		TweenService:Create(keyBox, TweenInfo.new(0.08), {
			Position = UDim2.fromOffset(35, 215)
		}):Play()

		task.wait(0.08)

		TweenService:Create(keyBox, TweenInfo.new(0.08), {
			Position = UDim2.fromOffset(25, 215)
		}):Play()
	end
end)

local dragging = false
local dragStart
local startPos

title.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		dragging = true
		dragStart = input.Position
		startPos = main.Position
	end
end)

UIS.InputChanged:Connect(function(input)
	if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
		local delta = input.Position - dragStart
		main.Position = UDim2.new(
			startPos.X.Scale,
			startPos.X.Offset + delta.X,
			startPos.Y.Scale,
			startPos.Y.Offset + delta.Y
		)
	end
end)

UIS.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		dragging = false
	end
end)
